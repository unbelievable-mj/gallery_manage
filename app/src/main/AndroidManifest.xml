<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      读取媒体。
      Android 13 (API 33) 起使用细粒度权限；30–32 回退到 READ_EXTERNAL_STORAGE。
      同时声明两个细粒度权限时，系统只会弹出一个合并对话框。
    -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!--
      免弹窗删除（Android 12 / API 31 起的特殊权限）。
      未授予时每次批量删除都要弹系统确认框，会打断滑卡节奏，
      因此在引导页作为「强烈推荐开启」项。
    -->
    <uses-permission android:name="android.permission.MANAGE_MEDIA" />

    <!-- 可选：读取照片未经编辑的 EXIF 定位信息 -->
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />

    <!-- 可选：后台清理任务的进度通知 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".SwipeGalleryApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SwipeGallery">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SwipeGallery">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
